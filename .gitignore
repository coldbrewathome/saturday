node_modules
dist
.DS_Store
*.tsbuildinfo
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.wrangler
worker/.wrangler
.playwright-mcp
.playwright-cli
data-site/dist
scripts/seo-audit-report.json
tmp

# Local debris (QA screenshots, agent output, tool caches)
/*.png
output
.antigravitycli
.claude/settings.local.json

# Local Places API usage ledger (spend guard state)
data/places-usage.json

# YouTube upload OAuth client secret + cached token (never commit)
video/youtube-oauth.json
video/.youtube-token.json

# Rendered video/audio assets (~486MB). The video/ build scripts, manifests and
# per-metro upload metadata ARE tracked; the renders they produce are not.
video/**/*.mp4
video/**/*.webm
video/**/*.mp3
video/**/*.wav
video/**/*.png

# Cron/agent run logs
data/*.log
